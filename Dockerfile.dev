FROM node:20-alpine

WORKDIR /app

# 依存関係のインストール用にpackage.jsonをコピー
COPY package*.json ./

# 依存関係をインストール
RUN npm install

# ソースコードをコピー（volumeマウントで上書きされる）
COPY . .

# ポート3000を公開
EXPOSE 3000

# 開発サーバーを起動
CMD ["npm", "run", "dev"]
